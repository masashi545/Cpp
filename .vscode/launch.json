{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "(lldb) Launch",
            "type": "lldb",
            "request": "launch",
            "program": "${fileDirname}/a.out",
            "args": [],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "build main.cpp"
        }
    ]
}